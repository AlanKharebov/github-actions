name: .NET Core Desktop4

on:
  push:

jobs:

  build:

    runs-on: ubuntu-latest  
    

    steps:
    - name: Checkout    
      uses: actions/checkout@v3
    - name: Decode the pfx
      run: |
        $pfx_cert_byte = "AAAA"
